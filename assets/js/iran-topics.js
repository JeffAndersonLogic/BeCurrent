// GENERATED by scripts/build-iran.js from scripts/lib/unit-content/iran.js.
// The shared Canvas record grammar is inserted from canvas-record-block.js.
// Never hand-edit this file; edit the content module or this renderer and rebuild.
(()=>{
  'use strict';

  const UNIT={"unit":"Iran at War","course":"CURRENT EVENTS","reviewed":"2026-09-04","terminalQuestion":"Was the 2026 Iran War mainly the result of recent decisions, or decades of unresolved conflict?"};
  const TOPICS={"topic-01":{"n":1,"key":"iran-t01","title":"The War They Are Watching","subtitle":"Current anchor · 2026","page":"index.html","overview":"Establish what is happening now, locate the pressure points, and separate the immediate trigger from the deeper conditions students will test across the unit.","learningTargets":[{"skill":"Geography and Power","target":"I can explain how Iran, Israel, U.S. regional positions, and the Strait of Hormuz shape the conflict."},{"skill":"Sourcing and Evidence","target":"I can separate a verifiable event from a claim about why a government acted."},{"skill":"Hypotheses","target":"I can propose a starting explanation and identify what evidence could change it."}],"successCriteria":[{"skill":"Geography and Power","criteria":"I can connect one location to a specific military, economic, or diplomatic consequence."},{"skill":"Sourcing and Evidence","criteria":"I can name one verified fact, one interpretation, and one unanswered historical question."},{"skill":"Hypotheses","criteria":"I can rank up to three possible causes and explain why one currently deserves the most weight."}],"questions":[{"skill":"Geography and Power","group":"geography","text":"Which piece of geography gives Iran the most leverage?"},{"skill":"Hypotheses","group":"prediction","text":"What do you think best explains how this conflict became possible?"},{"skill":"Cause and Effect","group":"reverse-reason","text":"Why did you choose those explanations?"},{"skill":"Sourcing and Evidence","group":"evidence","text":"Name one fact and one unanswered question."},{"skill":"Perspective and Interpretation","group":"perspective","text":"What evidence would help test either frame?"},{"skill":"Constructing Arguments","group":"claim","text":"What part of this conflict most needs a historical explanation?"}]},"topic-02":{"n":2,"key":"iran-t02","title":"The Coup","subtitle":"Reverse History stop · 1951–1953","page":"topic-02-1953.html","overview":"Examine oil nationalization, the political crisis around Mohammad Mosaddegh, documented U.S. and British covert involvement, and the event's long afterlife in Iranian political memory.","learningTargets":[{"skill":"Sourcing and Evidence","target":"I can use a declassified record to establish U.S. involvement without claiming the document proves everything about the coup."},{"skill":"Cause and Effect","target":"I can explain how oil, sovereignty, domestic politics, and Cold War fears interacted."},{"skill":"Contingency","target":"I can identify a decision that could plausibly have gone another way and explain why that matters."}],"successCriteria":[{"skill":"Sourcing and Evidence","criteria":"I can state one claim the archive supports and one claim it cannot establish by itself."},{"skill":"Cause and Effect","criteria":"I can connect the 1953 intervention to later distrust through a clear mechanism rather than calling later conflict inevitable."},{"skill":"Perspective and Interpretation","criteria":"I can explain why U.S. Cold War and Iranian sovereignty frames could both persist."}],"questions":[{"skill":"Cause and Effect","group":"reverse","text":"Why might nationalizing oil be more than an economic decision?"},{"skill":"Sourcing and Evidence","group":"evidence","text":"What can the declassified record prove?"},{"skill":"Perspective and Interpretation","group":"perspective","text":"Why could both frames persist?"},{"skill":"Contingency","group":"road","text":"What decision seems most contingent?"},{"skill":"Constructing Arguments","group":"claim","text":"How much weight should 1953 carry in an explanation of the 2026 war?"}]},"topic-03":{"n":3,"key":"iran-t03","title":"The Ally Becomes the Enemy","subtitle":"Reverse History stop · 1979","page":"topic-03-1979.html","overview":"Trace how opposition to the Shah became a broad revolution, how the Islamic Republic emerged, and how the hostage crisis created a powerful American grievance alongside Iran's memory of 1953.","learningTargets":[{"skill":"Cause and Effect","target":"I can explain how repression, rapid modernization, inequality, religion, and foreign influence combined in the revolution."},{"skill":"Perspective and Interpretation","target":"I can distinguish the Iranian grievance associated with 1953 from the American grievance created in 1979."},{"skill":"Comparison","target":"I can compare 1953 and 1979 using evidence and a causal mechanism."}],"successCriteria":[{"skill":"Cause and Effect","criteria":"I can explain why several groups opposed the Shah without pretending they wanted the same future."},{"skill":"Sourcing and Evidence","criteria":"I can use the opening-day hostage-crisis memorandum for what U.S. officials knew without treating it as proof of every participant's motive."},{"skill":"Comparison","criteria":"I can use one specific fact from each turning point to defend which changed the relationship more."}],"questions":[{"skill":"Cause and Effect","group":"ally","text":"Why can support for an ally create resentment toward the ally's supporter?"},{"skill":"Cause and Effect","group":"revolution","text":"Which pressure best explains why so many different groups could oppose the Shah at the same time?"},{"skill":"Sourcing and Evidence","group":"hostages","text":"How did the hostage crisis add something new to the relationship?"},{"skill":"Perspective and Interpretation","group":"perspective","text":"Which memory would make compromise harder later?"},{"skill":"Comparison","group":"claim","text":"Which transformed U.S.-Iran relations more: 1953 or 1979?"}]},"topic-04":{"n":4,"key":"iran-t04","title":"Why Fight Far From Home?","subtitle":"Reverse History stop · 1980–1988","page":"topic-04-security.html","overview":"Use the Iran-Iraq War to investigate why invasion, isolation, the IRGC, missiles, and relationships with armed groups became central to Iranian security strategy.","learningTargets":[{"skill":"Cause and Effect","target":"I can explain how the Iran-Iraq War shaped later security choices without treating it as the only cause."},{"skill":"Strategy","target":"I can distinguish deterrence, asymmetric strategy, and power projection."},{"skill":"Perspective and Interpretation","target":"I can explain why one strategy can look defensive to Iran and threatening to its neighbors."}],"successCriteria":[{"skill":"Cause and Effect","criteria":"I can write the mechanism connecting invasion and isolation to later missiles or regional armed partnerships."},{"skill":"Sourcing and Evidence","criteria":"I can use a specific wartime or institutional fact instead of relying on the label proxy."},{"skill":"Perspective and Interpretation","criteria":"I can acknowledge evidence for both deterrence and power projection before weighing them."}],"questions":[{"skill":"Cause and Effect","group":"invasion","text":"What lesson might leaders draw from surviving a long invasion?"},{"skill":"Cause and Effect","group":"mechanism","text":"What is the mechanism connecting the Iran-Iraq War to later deterrence?"},{"skill":"Sourcing and Evidence","group":"network","text":"Why is “proxy” useful — and where can the word mislead?"},{"skill":"Perspective and Interpretation","group":"perspective","text":"Can both descriptions be true at the same time?"},{"skill":"Constructing Arguments","group":"claim","text":"To what extent can Iran's modern regional military strategy be explained by the Iran-Iraq War?"}]},"topic-05":{"n":5,"key":"iran-t05","title":"The Nuclear Bargain","subtitle":"Reverse History thread · 1957–2018","page":"topic-05-nuclear.html","overview":"Follow Iran's nuclear program from U.S.-Iranian cooperation through the JCPOA, U.S. withdrawal, restored sanctions, reduced compliance, and the return of coercive pressure.","learningTargets":[{"skill":"Sourcing and Evidence","target":"I can explain what the JCPOA limited, what inspectors verified, and what the agreement left outside its scope."},{"skill":"Cause and Effect","target":"I can test the chain connecting U.S. withdrawal, sanctions, Iranian compliance, nuclear activity, and military pressure."},{"skill":"Policy Tradeoffs","target":"I can compare the risks of maintaining an imperfect agreement with the risks of abandoning it."}],"successCriteria":[{"skill":"Sourcing and Evidence","criteria":"I can describe the bargain as nuclear restrictions and monitoring in exchange for nuclear-related sanctions relief."},{"skill":"Cause and Effect","criteria":"I can identify the strongest and weakest arrows in the post-2018 causal chain."},{"skill":"Constructing Arguments","criteria":"I can argue whether the agreement's collapse made war more likely while acknowledging at least one other cause."}],"questions":[{"skill":"Historical Context","group":"origins","text":"Why does the U.S.-supported origin complicate a simple “Iran versus the world” nuclear story?"},{"skill":"Sourcing and Evidence","group":"jcpoa","text":"What problem did the JCPOA solve, and what problems did it deliberately leave outside the deal?"},{"skill":"Cause and Effect","group":"withdrawal","text":"Which arrow in the post-withdrawal chain is weakest?"},{"skill":"Policy Tradeoffs","group":"perspective","text":"Which risk seems larger: accepting an imperfect agreement or abandoning verified limits?"},{"skill":"Constructing Arguments","group":"claim","text":"Did the collapse of the nuclear agreement make eventual war substantially more likely?"}]},"topic-06":{"n":6,"key":"iran-t06","title":"From Shadow War to Open War","subtitle":"Reverse History stop · 2023–2026","page":"topic-06-escalation.html","overview":"Trace how covert and indirect conflict crossed into direct state-to-state attacks in 2024, expanded into major war in 2025, and produced the intermittent 2026 conflict.","learningTargets":[{"skill":"Turning Points","target":"I can distinguish a larger attack from an attack that changes what becomes thinkable next."},{"skill":"Cause and Effect","target":"I can explain how repeated retaliation can create path dependence without making war inevitable."},{"skill":"Perspective and Interpretation","target":"I can compare deterrence-as-restraint with deterrence-as-an-escalation ladder."}],"successCriteria":[{"skill":"Turning Points","criteria":"I can use one event from 2024 and one from 2025 or 2026 to identify a changed threshold."},{"skill":"Cause and Effect","criteria":"I can explain how one round altered the options or expectations in the next round."},{"skill":"Constructing Arguments","criteria":"I can defend one turning point while addressing the strongest evidence for a different one."}],"questions":[{"skill":"Strategy","group":"shadow","text":"Why might rivals prefer a “shadow war” to direct war?"},{"skill":"Turning Points","group":"direct","text":"What changed in 2024 even though neither side chose full-scale war yet?"},{"skill":"Cause and Effect","group":"war","text":"Which matters more: the size of an escalation or the precedent it creates?"},{"skill":"Perspective and Interpretation","group":"perspective","text":"Which logic best describes 2024–2026?"},{"skill":"Constructing Arguments","group":"claim","text":"At what point between 2018 and 2026 did full-scale war become most likely?"}]},"topic-07":{"n":7,"key":"iran-t07","title":"The Hormuz Lever","subtitle":"Geography becomes power","page":"topic-07-hormuz.html","overview":"Connect the Strait of Hormuz's chokepoint geography to the 1980s Tanker War, the measured collapse in 2026 oil flows, and the waterway's military, economic, and diplomatic uses.","learningTargets":[{"skill":"Geography and Power","target":"I can explain how a narrow waterway gives Iran leverage without requiring naval equality."},{"skill":"Quantitative Evidence","target":"I can use the change in estimated oil flows as evidence without claiming the numbers prove motive."},{"skill":"Comparison","target":"I can identify a continuity and a difference between the Tanker War and the 2026 Hormuz crisis."}],"successCriteria":[{"skill":"Geography and Power","criteria":"I can complete the chain from narrow route to disrupted shipping to consequences beyond the Gulf."},{"skill":"Quantitative Evidence","criteria":"I can explain what the 21.6 to 4.9 million-barrel change establishes and what it cannot establish."},{"skill":"Constructing Arguments","criteria":"I can decide whether Hormuz is primarily military, economic, or diplomatic leverage and support that ranking."}],"questions":[{"skill":"Geography and Power","group":"map","text":"Explain the chokepoint mechanism."},{"skill":"Comparison","group":"tankers","text":"What does the Tanker War help explain about 2026?"},{"skill":"Quantitative Evidence","group":"data","text":"What does the 21.6 → 4.9 change prove, and what does it not prove?"},{"skill":"Perspective and Interpretation","group":"perspective","text":"Which actor has the strongest reason to avoid a prolonged closure?"},{"skill":"Constructing Arguments","group":"claim","text":"Is Hormuz primarily a military weapon, an economic weapon or a diplomatic bargaining tool?"}]},"topic-08":{"n":8,"key":"iran-t08","title":"When Did This War Really Begin?","subtitle":"Causation synthesis","page":"topic-08-synthesis.html","overview":"Rebuild the entire causal chain, rank the most important turning points, test the strongest competing explanation, and write a final answer to the unit question.","learningTargets":[{"skill":"Cause and Effect","target":"I can connect evidence from several points in the chain through explicit causal mechanisms."},{"skill":"Counterargument","target":"I can state the strongest case for a turning point I did not rank first."},{"skill":"Constructing Arguments","target":"I can distinguish the literal beginning of war from the explanatory beginning I defend."}],"successCriteria":[{"skill":"Cause and Effect","criteria":"I can use at least three specific pieces of evidence from different points in the chain."},{"skill":"Counterargument","criteria":"I can explain why a competing turning point matters before showing why mine carries more weight."},{"skill":"Constructing Arguments","criteria":"I can name an explanatory beginning, define what it explains, and connect each piece of evidence to the final claim."}],"questions":[{"skill":"Cause and Effect","group":"chain","text":"Choose three consecutive stops and write the arrows between them."},{"skill":"Causal Ranking","group":"ranking","text":"Rank your three strongest turning points and explain why each belongs where it does."},{"skill":"Counterargument","group":"counter","text":"Write the best case for a turning point you did not rank first."},{"skill":"Scope and Definition","group":"definition","text":"What exactly does your chosen “beginning” explain?"},{"skill":"Constructing Arguments","group":"claim","text":"Was the 2026 Iran War mainly the result of recent decisions, or decades of unresolved conflict?"}]}};
  const body=document.body;
  const topic=body.dataset.topic||'topic-01';
  const meta=TOPICS[topic];
  if(!meta)return;
  const prefix='bcv2-iran-'+topic+'-';
  const modeButtons=[...document.querySelectorAll('[data-mode]')];

  function esc(value){
    return String(value==null?'':value).replace(/&/g,'&amp;').replace(/</g,'&lt;')
      .replace(/>/g,'&gt;').replace(/"/g,'&quot;');
  }

  function installPosition(){
    if(document.querySelector('.ir-position'))return;
    const anchor=document.querySelector('.sticky-journey')||document.querySelector('.mast');
    if(!anchor)return;
    const bar=document.createElement('div');
    bar.className='ir-position';
    bar.innerHTML='<div class="ir-position-inner"><div><div class="ir-position-series">IRAN AT WAR · REVERSE HISTORY INVESTIGATION</div><div class="ir-position-title">Topic '+meta.n+' · '+esc(meta.title)+'</div><span class="ir-position-series">'+esc(meta.subtitle)+'</span></div><div class="ir-position-count"><strong>'+meta.n+'</strong> of 8 topics</div></div>';
    anchor.insertAdjacentElement('afterend',bar);
  }

  body.classList.add('iran-academic-v2');

  function installObjectives(){
    if(document.getElementById('objectives'))return;
    const hero=document.querySelector('.ir-hero');
    if(!hero)return;
    const section=document.createElement('section');
    section.className='ir-section ir-objectives';
    section.id='objectives';
    const targets=meta.learningTargets.map(t=>'<li><strong>'+esc(t.skill)+'.</strong> '+esc(t.target)+'</li>').join('');
    const criteria=meta.successCriteria.map(c=>'<li><strong>'+esc(c.skill)+'.</strong> '+esc(c.criteria)+'</li>').join('');
    section.innerHTML='<details><summary><span><span class="ir-kicker red">Today\'s direction</span><strong>Learning targets and success criteria</strong></span><span aria-hidden="true">+</span></summary><div class="ir-objective-grid"><div><h2>Learning targets</h2><ol>'+targets+'</ol></div><div><h2>Success criteria</h2><ol>'+criteria+'</ol></div></div></details>';
    hero.insertAdjacentElement('afterend',section);
  }

  function setMode(mode){
    body.classList.toggle('teacher-on',mode==='teacher');
    modeButtons.forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.mode===mode)));
    try{localStorage.setItem('bcv2-mode',mode)}catch(_){ }
  }
  let savedMode='student';
  try{savedMode=localStorage.getItem('bcv2-mode')||'student'}catch(_){ }
  modeButtons.forEach(b=>b.addEventListener('click',()=>setMode(b.dataset.mode)));
  setMode(savedMode);
  installPosition();
  installStudyGuide();
  installObjectives();

  const groups=[...document.querySelectorAll('[data-group]')];

  function read(key){
    try{
      const raw=localStorage.getItem(prefix+key);
      return raw===null?null:JSON.parse(raw);
    }catch(_){return null}
  }
  function write(key,value){
    try{localStorage.setItem(prefix+key,JSON.stringify(value));return true}catch(_){return false}
  }
  function valueOf(group){
    if(group.matches('textarea,input[type="text"],input[type="url"]'))return group.value;
    return [...group.querySelectorAll('input:checked')].map(i=>i.value);
  }
  function textValue(group){
    const value=valueOf(group);
    return Array.isArray(value)?value.join('; '):String(value||'');
  }
  function restore(group,value){
    if(value===null)return;
    if(group.matches('textarea,input[type="text"],input[type="url"]'))group.value=value||'';
    else if(Array.isArray(value))group.querySelectorAll('input').forEach(i=>i.checked=value.includes(i.value));
  }
  groups.forEach(g=>restore(g,read(g.dataset.group)));

  document.querySelectorAll('[data-max]').forEach(group=>{
    const max=Number(group.dataset.max)||0;
    if(!max)return;
    function paint(){
      const counter=group.parentElement.querySelector('[data-choice-count]');
      if(counter)counter.textContent=[...group.querySelectorAll('input:checked')].length+' of '+max+' selected';
    }
    group.addEventListener('change',event=>{
      if(!event.target.matches('input[type="checkbox"]'))return;
      if([...group.querySelectorAll('input:checked')].length>max)event.target.checked=false;
      paint();
    });
    paint();
  });

  function filled(key){
    const value=read(key);
    return Array.isArray(value)?value.length>0:typeof value==='string'&&value.trim().length>0;
  }
  function updateProgress(){
    const keys=meta.questions.map(q=>q.group);
    const done=keys.filter(filled).length;
    document.querySelectorAll('[data-topic-progress]').forEach(el=>el.textContent=done+' of '+keys.length+' saved');
  }

  // Save continuously so leaving the page after typing cannot strand the last
  // paragraph. The visible buttons remain as an explicit reassurance and status.
  groups.forEach(group=>{
    const eventName=group.matches('textarea,input[type="text"],input[type="url"]')?'input':'change';
    group.addEventListener(eventName,()=>{
      write(group.dataset.group,valueOf(group));
      updateProgress();
    });
  });

  document.querySelectorAll('[data-save]').forEach(btn=>btn.addEventListener('click',()=>{
    const key=btn.dataset.save;
    const group=document.querySelector('[data-group="'+key+'"]');
    if(!group)return;
    const ok=write(key,valueOf(group));
    const status=document.querySelector('[data-status="'+key+'"]');
    if(status){
      status.textContent=ok?'Saved on this Chromebook':'Could not save in this browser';
      if(ok)setTimeout(()=>{status.textContent=''},2400);
    }
    updateCompletion();
  }));

  // ── The Canvas record grammar ─────────────────────────────────────────────────
  //
  // GENERATED from scripts/lib/canvas-record-block.js. Never hand-edit this block:
  // the week renderer and all of the generated briefs carry the same copy, and
  // scripts/validate.js fails the build when one of them drifts.
  //
  // The paste is the only evidence that reaches the teacher, and without this
  // footer a truncated, half-empty or hand-edited one is indistinguishable from a
  // good one. A blank paste that still carries every heading reads as "student
  // wrote nothing" when the real cause is a wiped localStorage.
  //
  // Format is deliberately dumb. Canvas's editor rewrites HTML, so nothing may
  // depend on a tag, an attribute or a class surviving. Every record is one
  // self-delimiting line that a regex recovers from the submission's text content
  // even if every newline collapses.
  //
  // The `#BHV|` and `#BHR|` tokens are shared with BeHistorical on purpose so one
  // parser serves both courses. The sentinel is not, because that line is visible
  // in the student's paste. See the header of scripts/lib/canvas-parse-core.js.
  var BC_RECORD_VERSION = 1;
  var BC_RECORD_OPEN = "--- BECURRENT RECORD, do not edit ---";
  var BC_RECORD_CLOSE = "--- END BECURRENT RECORD ---";

  function bcEsc(value) {
    return String(value == null ? '' : value)
      .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;');
  }

  // Canvas rewrites line breaks on the way in and again on the way out, so a hash
  // over raw text would not survive its own round trip. Whitespace is collapsed
  // before hashing: the check is "is this the same writing", not "are the newlines
  // byte-identical".
  function bcNormalizeForHash(value) {
    return String(value == null ? '' : value).replace(/\s+/g, ' ').trim();
  }

  // FNV-1a, 32-bit. Small, dependency-free, and identical to bhHash() in the Node
  // parser. This detects accident and drift; it is not a tamper-proof signature,
  // and nothing downstream should treat it as one.
  function bcHash(value) {
    var s = bcNormalizeForHash(value);
    var h = 0x811c9dc5;
    for (var i = 0; i < s.length; i++) {
      h ^= s.charCodeAt(i);
      h = (h + ((h << 1) + (h << 4) + (h << 7) + (h << 8) + (h << 24))) >>> 0;
    }
    return ('0000000' + h.toString(16)).slice(-8);
  }

  function bcWordCount(value) {
    var s = bcNormalizeForHash(value);
    return s ? s.split(' ').length : 0;
  }

  // `|` and newlines are the record format's only reserved characters.
  function bcField(value) {
    return String(value == null ? '' : value).replace(/[|\r\n]+/g, ' ').trim();
  }

  // Each paragraph its own <p>. The parser reads text content, so Canvas dropping
  // the styling is fine, but it must not flatten a student's blank line: a blank
  // line between paragraphs has to survive as two paragraphs, which is why the
  // response is emitted as sibling <p> elements rather than with <br>.
  //
  // `wrap` is the inline tag the response is set in, or '' for none. The Brief
  // sets responses in italics so the teacher can tell the student's writing from
  // the prompt above it at a glance; the week page passes '' and stays plain.
  //
  // A blank answer emits NOTHING, and that is not laziness. Everything between the
  // "My response:" marker and the next label is what the parser hashes back against
  // the footer's `rh`, so a friendly "No response recorded." placeholder sitting
  // there hashes as writing: the parser reports the answer as EDITED, which is an
  // accusation, and never reports it as BLANK, which is the truth. The student is
  // told about blanks by the gather status line, and the teacher by `w=0` in the
  // footer, both of which are outside the hashed region.
  function bcParagraphsHtml(text, wrap) {
    var open = wrap ? '<' + wrap + '>' : '';
    var close = wrap ? '</' + wrap + '>' : '';
    var parts = String(text || '').split(/\n{2,}/).map(function (p) { return p.trim(); })
      .filter(Boolean);
    return parts.map(function (p) {
      return '<p>' + open + bcEsc(p).replace(/\n/g, '<br>') + close + '</p>';
    }).join('');
  }

  /**
   * One header line, then one line per gathered response, between the sentinels.
   *
   * @param {Array}  work  [{ ord, id, label, prompt, text, confidence }]
   * @param {Object} opts  { topic, expected, isoStamp }
   *
   * `opts.expected` is how many slots the surface DEFINES, and it must always be
   * computed by the caller. A literal reports a week whose brief is not published
   * yet as three answers short, and a wrong denominator is worse than none.
   */
  function bcRecordManifest(work, opts) {
    var o = opts || {};
    var rows = work.map(function (w) {
      return {
        ord: bcField(w.ord || 'xx'),
        slot: bcField(w.id),
        label: bcField(w.label),
        words: bcWordCount(w.text),
        chars: bcNormalizeForHash(w.text).length,
        promptHash: bcHash(w.prompt),
        responseHash: bcHash(w.text),
        confidence: w.confidence || ''
      };
    });

    // Sum over the per-response hashes, so deleting a whole record line breaks it
    // too, not just editing the writing inside one.
    var sum = bcHash(rows.map(function (r) { return r.slot + ':' + r.responseHash; }).join('|'));

    var header = '#BHV|v=' + BC_RECORD_VERSION
      + '|topic=' + bcField(o.topic)
      + '|copied=' + o.isoStamp
      + '|items=' + rows.length
      + '|expected=' + Number(o.expected || 0)
      + '|sum=' + sum + '|#';

    var lines = rows.map(function (r) {
      return '#BHR|i=' + r.ord
        + '|slot=' + r.slot
        + '|lab=' + r.label
        + '|w=' + r.words
        + '|c=' + r.chars
        + '|ph=' + r.promptHash
        + '|rh=' + r.responseHash
        + '|cf=' + r.confidence + '|#';
    });

    return [BC_RECORD_OPEN, header].concat(lines).concat([BC_RECORD_CLOSE]);
  }

  // Each line its own <p>. Canvas may drop the styling and that is fine, nothing
  // parses the presentation.
  function bcRecordFooterHtml(lines) {
    return '<hr>' + lines.map(function (line) {
      return '<p style="font-family:monospace;font-size:.68rem;opacity:.6;margin:.15rem 0;">'
        + bcEsc(line) + '</p>';
    }).join('');
  }

  function gatherRows(){
    return meta.questions.map((q,i)=>{
      const group=document.querySelector('[data-group="'+q.group+'"]');
      return {
        n:i+1,
        ord:'xx',
        id:'iran-'+topic+'-'+q.group,
        label:'Iran at War, Topic '+meta.n+', Filing '+(i+1),
        prompt:q.text,
        text:group?textValue(group).trim():'',
        confidence:''
      };
    });
  }

  function buildDocument(){
    const rows=gatherRows();
    const stamp=new Date();
    const dateline=UNIT.course+' · '+UNIT.unit+' · Topic '+meta.n;
    const head='<p><strong>'+esc(dateline)+'</strong></p><h2>'+esc(meta.title)+'</h2><p><em>Student work, copied '+esc(stamp.toLocaleString())+'</em></p><hr>';
    const bodyHtml=rows.map(r=>'<h3>'+esc(r.label)+'</h3><p><strong>Question: '+esc(r.prompt)+'</strong></p><p><strong>My response:</strong></p>'+bcParagraphsHtml(r.text,'em')).join('<hr>');
    const manifest=bcRecordManifest(rows,{topic:meta.key,expected:rows.length,isoStamp:stamp.toISOString()});
    const plain=[dateline,meta.title,'Student work, copied '+stamp.toLocaleString(),'']
      .concat(rows.map(r=>[r.label.toUpperCase(),'Question: '+r.prompt,'My response:',r.text,''].join('\n')))
      .concat(manifest).join('\n');
    return {html:head+bodyHtml+bcRecordFooterHtml(manifest),plain,count:rows.filter(r=>r.text).length,total:rows.length};
  }

  function say(message){
    const status=document.getElementById('iran-gather-status');
    if(status)status.textContent=message;
  }

  function selectOutput(out){
    try{
      const range=document.createRange();range.selectNodeContents(out);
      const sel=window.getSelection();sel.removeAllRanges();sel.addRange(range);return true;
    }catch(_){return false}
  }

  function gather(){
    const out=document.getElementById('iran-gather-output');
    if(!out)return null;
    const doc=buildDocument();
    out.innerHTML=doc.html;out.dataset.plain=doc.plain;
    const blank=doc.total-doc.count;
    say('Gathered '+doc.count+' of '+doc.total+' filings.'+(blank?' '+blank+' still blank. Complete '+(blank===1?'it':'them')+' and gather again.':' Copy this, then paste it into Canvas.'));
    return doc;
  }

  function copyFallback(out){
    let copied=false;try{copied=document.execCommand('copy')}catch(_){copied=false}
    if(copied){say('Copied with formatting. Paste it into Canvas.');return}
    if(navigator.clipboard&&navigator.clipboard.writeText){
      navigator.clipboard.writeText(out.dataset.plain||out.textContent||'')
        .then(()=>say('Copied as plain text. Paste it into Canvas.'))
        .catch(()=>say('Copy is blocked. Your work is selected; press Ctrl+C or Cmd+C.'));
    }else say('Your work is selected; press Ctrl+C or Cmd+C.');
  }

  function copy(){
    const out=document.getElementById('iran-gather-output');if(!out)return;
    if(!out.dataset.plain)gather();
    selectOutput(out);
    const html=out.innerHTML,plain=out.dataset.plain||'';
    if(window.ClipboardItem&&navigator.clipboard&&navigator.clipboard.write){
      navigator.clipboard.write([new ClipboardItem({'text/html':new Blob([html],{type:'text/html'}),'text/plain':new Blob([plain],{type:'text/plain'})})])
        .then(()=>say('Copied with formatting. Paste it into Canvas.'))
        .catch(()=>copyFallback(out));
    }else copyFallback(out);
  }

  function installGather(){
    if(document.getElementById('gather'))return;
    const section=document.createElement('section');
    section.className='ir-section';section.id='gather';
    section.innerHTML='<div class="ir-gather"><div class="ir-kicker red">Save your work</div><h2>Gather This Topic</h2><p>Your filings save only in this browser on this device. Gather them into one checked record, copy it, and paste it into the Canvas assignment.</p><div class="ir-gather-actions"><button class="ir-save" type="button" data-gather-topic>Gather This Topic</button><button class="ir-save secondary" type="button" data-copy-topic>Copy to Clipboard</button><button class="ir-clear" type="button" data-clear-topic>Clear This Topic</button><span class="ir-progress" data-topic-progress></span></div><p id="iran-gather-status" class="ir-status" role="status" aria-live="polite"></p><div id="iran-gather-output" class="ir-gather-output" tabindex="0" aria-label="Gathered work"><p class="ir-gather-placeholder">Gather your filings, copy them, then paste into Canvas.</p></div></div>';
    const sources=document.getElementById('sources');
    if(sources)sources.insertAdjacentElement('beforebegin',section);
    else document.querySelector('.ir-shell').appendChild(section);
    section.querySelector('[data-gather-topic]').addEventListener('click',gather);
    section.querySelector('[data-copy-topic]').addEventListener('click',copy);
    section.querySelector('[data-clear-topic]').addEventListener('click',()=>{
      if(!confirm('Clear the saved work for this Iran topic on this Chromebook?'))return;
      meta.questions.forEach(q=>{try{localStorage.removeItem(prefix+q.group)}catch(_){ }});
      location.reload();
    });
  }

  // Remote historical images degrade to a local, labelled fallback instead of
  // leaving a broken frame on a student page.
  document.querySelectorAll('.ir-map img[src^="http"]').forEach(img=>img.addEventListener('error',()=>{
    img.src=img.alt&&/map|strait|gulf/i.test(img.alt)?'../assets/images/week-art/map-fallback.svg':'../assets/images/week-art/card-fallback.svg';
    img.alt='Local fallback illustration; the remote historical image is unavailable.';
  },{once:true}));

  installGather();
  updateProgress();
})();
